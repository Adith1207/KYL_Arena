import LoginPage from "./LoginClient";

export const metadata = {
  title: "Login",
  description: "Stop checking Strava profiles manually. Sign in to KYL Arena, sync your activities, and start competing.",
};

export default function Page() {
  return <LoginPage />;
}
